{% extends 'base.html.twig' %}

{% block title %}StudentCourse{% endblock %}

{% block body %}
    <h1>StudentCourse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student_course.id }}</td>
            </tr>
            <tr>
                <th>NberCourses</th>
                <td>{{ student_course.nberCourses }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ student_course.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_student_course_index') }}">back to list</a>

    <a href="{{ path('app_student_course_edit', {'id': student_course.id}) }}">edit</a>

    {{ include('student_course/_delete_form.html.twig') }}
{% endblock %}
