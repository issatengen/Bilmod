<!-- Mini Header with Dropdown Menus -->
<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm rounded mb-4">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="#">Admin Panel</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#miniHeaderNav" aria-controls="miniHeaderNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="miniHeaderNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <!-- Categories Dropdown -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="categoriesDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Categories
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="categoriesDropdown">
                        <li><a class="dropdown-item" href="{{ path('app_category_index') }}">Liste des Categories</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_category_new') }}">Ajouter</a></li>
                    </ul>
                </li>
                <!-- Users Dropdown -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="usersDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Users
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="usersDropdown">
                        <li><a class="dropdown-item" href="{{ path('app_admin_users_index') }}">Liste des utilisateurs</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_admin_users_new') }}">Ajouter</a></li>
                    </ul>
                </li>
                <!-- Lessons Dropdown -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="lessonsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Lessons
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="lessonsDropdown">
                        <li><a class="dropdown-item" href="{{ path('app_leson_index') }}">List Lessons</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_leson_new') }}">Add Lesson</a></li>
                    </ul>
                </li>
                <!-- Students Dropdown -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="studentsDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Students
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="studentsDropdown">
                        <li><a class="dropdown-item" href="{{ path('app_student_index') }}">Liste Etudiants</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_student_new') }}">Validé un étudiant</a></li>
                    </ul>
                </li>
                <!-- Courses Dropdown -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="coursesDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Courses
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="coursesDropdown">
                        <li><a class="dropdown-item" href="{{ path('app_course_index') }}">Liste des Cours</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_course_new') }}">Ajouter</a></li>
                    </ul>
                </li>
            </ul>
            <!-- User Info -->
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            {{ app.user.email }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                            <li><span class="dropdown-item-text">Roles: {{ app.user.roles|join(', ') }}</span></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="{{ path('app_logout') }}">
                                    <i class="bi bi-box-arrow-left"></i>
                                    Logout
                                </a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="btn btn-success m-2" href="{{ path('app_login') }}">
                            <i class="bi bi-box-arrow-right"></i>
                            Login
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>