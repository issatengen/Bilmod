{% extends 'base.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}

    <main class="main shadow-sm p-3 mb-5 bg-light rounded container">
            {{include('partials/adminHeader.html.twig') }}
            <p class="h3">Ajouter une Categorie.</p>
            <div class="container">
                <a href="{{ path('app_category_index') }}" class="btn btn-light shadow-sm text-decoration-none text-info">
                    <i class="bi bi-arrow-left"></i>
                    Back to list
                </a>                
                <div class="row mt-4">
                    <div class="col-md-2">
                        <h1 class="mb-4">
                        <i class="bi bi-people-add-fill"></i>
                        </h1>
                    </div>
                    <div class="col-md-6">
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6 bg-white rounded shadow-sm">
                        <!-- Grid Item 2 -->
                        <div class="m-3">
                            <div class="card-body bg-light p-2 shadow-sm">
                                {{ include('category/_form.html.twig') }}
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <!-- Grid Item 3 -->
                        <div class="mb-3">
                            <div class="text-center pt-4">
                                <i class="bi bi-bookmark-plus text-danger" style="font-size: 5rem;"></i>
                                <h5 class="card-title">Ajouter une catégorie de formation</h5>
                                <p class="card-text">Merci</p>
                            </div>
                        </div>
                    </div>
                </div>

                <a href="{{ path('app_category_index') }}">back to list</a>
            </div>
        </main>
{% endblock %}
