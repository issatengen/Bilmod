{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}

    <div class="main shadow-sm p-3 mb-5 bg-light rounded container">
        {{include('partials/adminHeader.html.twig') }}
        <p class="h3">Modifier la catégorie</p>
        <div class="row mb-4 mt-4">
            <div class="col-md-6 rounded bg-white p-4 ms-2 shadow-sm">
                <a href="{{ path('app_category_index') }}">back to list</a>
                {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
            <div class="col-md-4">
                <div class="text-center pt-3">
                    <i class="bi bi-pencil text-danger" style="font-size: 5rem;"></i>
                    <h5 class="card-title">Modifier</h5>
                    <p class="card-text">Merci</p>
                    {{ include('category/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
