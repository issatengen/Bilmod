{% extends 'base.html.twig' %}

{% block title %}Leson{% endblock %}

{% block body %}
    <h1>Leson</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ leson.id }}</td>
            </tr>
            <tr>
                <th>LesonCode</th>
                <td>{{ leson.lesonCode }}</td>
            </tr>
            <tr>
                <th>LesonTitle</th>
                <td>{{ leson.lesonTitle }}</td>
            </tr>
            <tr>
                <th>TimeAllocated</th>
                <td>{{ leson.timeAllocated }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_leson_index') }}">back to list</a>

    <a href="{{ path('app_leson_edit', {'id': leson.id}) }}">edit</a>

    {{ include('leson/_delete_form.html.twig') }}
{% endblock %}
